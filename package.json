{
  "type": "module",
  "devDependencies": {
    "vitepress": "1.0.0-rc.10"
  },
  "scripts": {
    "dev": "vitepress dev --host=0.0.0.0 --port=8080",
    "build": "vitepress build",
    "preview": "vitepress preview"
  }
}